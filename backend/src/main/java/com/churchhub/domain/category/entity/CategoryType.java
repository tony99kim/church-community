package com.churchhub.domain.category.entity;

public enum CategoryType {
    NOTICE,     // 공지 (운영진만 작성)
    FREE,       // 자유게시판
    GATHERING,  // 소모임 모집
    COMMUNITY,  // 기존 유지
    EVENT,      // 행사 (기존 유지)
    LOCAL       // 지역 (기존 유지)
}
